7) Write a C program to print the Fibonacci series up to n terms using while loop.

#include<stdio.h>
	int n, i = 1;
	int first = 0, second = 1, next;

	// Input number of terms
	printf("Enter number of terms: ");
	scanf("%d", &n);

	printf("Fibonacci Series: \n");

	// Print Fibonacci using while loop
	while (i <= n) {
		printf("%d ", first);
	}