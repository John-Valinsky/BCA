5) Write a C program which input three numbers and display the largest number using conditional operator.

#include <stdio.h>

    int main(){
        int a, b,c, largest;

        // Input three numbers
        printf("Enter three numbers: ");
        scanf("%d %d %d", &a, &b, &c);

        // Find largest using conditional (ternary) operator
        largest = (a > b) ?

        // Display result
        
    }