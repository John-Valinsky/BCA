10) Write a C program using insertion Sort.

#include <stdio.h>

int main() {
    int arr[100], n, j, key;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter elements:\n");
}